package model.enumfields;

/**
 * The {@link GenreType} enum class contains
 * the standardized options of the Genre field in
 * the {@link model.Book} class.
 */
public enum GenreType {
    FICTION,
    NON_FICTION,
    POETRY,
    DRAMA
}
