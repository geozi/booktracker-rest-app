package model.enumfields;

/**
 * The {@link FormatType} enum class contains
 * the standardized options of the Format field in
 * the {@link model.Edition} class.
 */
public enum FormatType {
    HARDCOVER,
    PAPERBACK,
    EBOOK
}
